var path = require('path');
var app = require(path.join(__dirname, '..', 'app'));
var port = process.env.PORT || 3000;
var onBind = function () {
    console.log('Server running');
};
app.listen(port, onBind);